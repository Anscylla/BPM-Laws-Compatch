# BPM / Laws+ Compatch - prawa Laws+ przeniesione na model BPM
# Bazuja na aktualnych definicjach Laws+, dopisane sa:
#  * country_rigidity_baseline_add - stat wylacznie BPM, bez niego kazde prawo
#    Laws+ jest neutralne dla sztywnosci politycznej, wokol ktorej BPM balansuje gre;
#  * institution / institution_modifier tam, gdzie BPM wiaze instytucje z cala grupa praw.
# WYGENEROWANE: python tools/gen_lp_laws.py

# --- law_class_collaboration  (zrodlo: Laws+/01_economic_system.txt) ---
REPLACE:law_class_collaboration = { # 계급협조	# Laws +
	group = lawgroup_economic_system

	icon = "gfx/interface/icons/law_icons/class_collaboration.dds"

	progressiveness = 75

	disallowing_laws = {
		law_corporatocracy
		law_serfdom
		law_women_in_the_fields
		law_combination_acts
		law_right_to_associate
		law_factory_councils
	}

	unlocking_laws = {
		law_corporate_state
		law_social_monarchy
	}

	on_activate = {
	}

	modifier = {
		state_capitalists_investment_pool_contribution_add = 0.15
		building_nationalization_cost_mult = -0.5
		building_nationalization_radicals_mult = -0.5
		country_nationalization_cost_non_members_mult = -0.25
		country_government_dividends_efficiency_add = 0.2
		country_government_dividends_reinvestment_add = 0.25
		country_private_construction_allocation_mult = 0.4
		building_company_worker_dividends_add = 0.25
		country_max_companies_add = 1
		country_free_charters_add = 1
		country_rigidity_baseline_add = 5	# BPM compat
	}

	ai_enact_weight_modifier = { #청원
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}

		if = {
			limit = {
				OR = {
					has_law_or_variant = law_type:law_corporate_state
					has_law_or_variant = law_type:law_social_monarchy
				}
			}
			add = 400
		}
	}

	ai_impose_chance = {
		value = 0

		if = {
			limit = {
				has_law = law_type:law_class_collaboration
			}

			add = base_impose_law_weight
			multiply = 10.0
		}

		if = {
			limit = {
				scope:target_country = {
					c:BIC ?= this
					OR = {
						has_journal_entry = je_british_dictate_plantations
						has_journal_entry = je_british_dictate_military
						has_journal_entry = je_british_dictate_universities
					}
				}
			}
			multiply = 0
		}
	}
	institution = institution_economy	# BPM compat
	institution_modifier = {
		country_private_construction_allocation_mult = -0.05
		state_capitalists_investment_pool_efficiency_mult = 0.05
		state_shopkeepers_investment_pool_efficiency_mult = 0.05
		state_laborers_investment_pool_efficiency_mult = 0.05
		state_machinists_investment_pool_efficiency_mult = 0.05
		country_government_dividends_efficiency_add = 0.08
	}
}

# --- law_corporatocracy  (zrodlo: Laws+/00_governance_principles.txt) ---
REPLACE:law_corporatocracy = { # 기업 국가	# Laws +
	group = lawgroup_governance_principles
	
	icon = "gfx/interface/icons/law_icons/corporatocracy.dds"
	
	progressiveness = 0

	unlocking_technologies = {
		corporate_management
	}

	unlocking_laws = {
		law_laissez_faire
	}

	can_enact = {
	}

	on_activate = {
		transition_off_of_monarchy_effects = yes
		if = {
			limit = {
				c:POR ?= this
				ruler ?= {
					has_template = POR_manuel_ii_de_braganca_template
				}
			}
			trigger_event = { id = portuguese_monarchs.3 }
		}
	}

	can_impose = {
		OR = {
			AND = {
				is_in_same_power_bloc = scope:target_country
				AND = {
					modifier:country_can_impose_same_lawgroup_governance_principles_in_power_bloc_bool = yes
					has_law = scope:law
				}
			}
			can_impose_law_default = yes
		}
	}

	modifier = {
		country_legitimacy_ideological_incoherence_mult = 0.2
		state_political_strength_from_wealth_mult = 1
		interest_group_ig_industrialists_pol_str_mult = 0.25
		country_max_companies_add = 1
		country_free_charters_add = 3
		country_forbid_monopoly_bool = no
		country_rigidity_baseline_add = 10	# BPM compat
	}

	ai_will_do = {
		exists = ruler
		ruler = {
			has_ideology = ideology:ideology_anarcho_liberal
		}
	}

	ai_enact_weight_modifier = { #청원
		value = 0
		
		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}

	ai_impose_chance = {
		value = 0
		
		if = {
			limit = {
				has_law = law_type:law_corporatocracy
				has_strategy = ai_strategy_reactionary_agenda
			}

			add = base_impose_law_weight
			multiply = 5.0
		}
			
		if = {
			limit = {
				scope:target_country = {
					c:BIC ?= this
					OR = {	
						has_journal_entry = je_british_dictate_plantations
						has_journal_entry = je_british_dictate_military
						has_journal_entry = je_british_dictate_universities
					}
				}
			}
			multiply = 0
		}
	}
}

# --- law_direct_democracy  (zrodlo: Laws+/ballot_system.txt) ---
REPLACE:law_direct_democracy = { # 직접 민주제
	group = lawgroup_ballot_system

	icon = "gfx/interface/icons/law_icons/direct_democracy.dds"

	progressiveness = 200

	unlocking_laws = {
		law_proletariat_dictatorship
		law_anarchy
		law_universal_suffrage
	}

	unlocking_technologies = {
		political_agitation
	}

	disallowing_laws = {
		law_outlawed_dissent
		law_censorship
	}

	modifier = {
		country_authority_add = -100
		country_legitimacy_govt_total_clout_add = 20
		country_law_enactment_time_mult = -0.5
		political_movement_pop_attraction_mult = 0.5
		country_rigidity_baseline_add = -15	# BPM compat
	}

	on_activate = {
	}

	ai_enact_weight_modifier = { #청원
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 75
		}
	}
}

# --- law_early_industrialization  (zrodlo: Laws+/01_economic_system.txt) ---
REPLACE:law_early_industrialization = { # 초기 산업화	# Laws +
	group = lawgroup_economic_system
	parent = law_traditionalism
	icon = "gfx/interface/icons/law_icons/early_industrialization.dds"

	progressiveness = -50

	is_visible = {
		c:RUS ?= this
		has_law = law_type:law_early_industrialization
	}

	on_activate = {
	}

	modifier = {
		state_shopkeepers_investment_pool_efficiency_mult = -0.5
		state_farmers_investment_pool_efficiency_mult = -0.5
		state_clergymen_investment_pool_efficiency_mult = -0.5
		state_bureaucracy_population_base_cost_factor_mult = -0.25
		country_government_dividends_reinvestment_add = 0.25
		country_private_construction_allocation_mult = 0.25
		building_nationalization_investment_return_add = 0.1
		state_market_access_price_impact = -0.1
		building_self_investment_chance_add = 0.25
		country_disable_nationalization_without_compensation_bool = yes
		country_rigidity_baseline_add = 8	# BPM compat
	}

	can_impose = {
		always = no
	}

	# AI는 결코 전통주의로 돌아가지 않을 것임
	ai_will_do = {
		always = no
	}

	ai_enact_weight_modifier = { #청원
		value = 0
	}
	institution = institution_economy	# BPM compat
	institution_modifier = {
		state_aristocrats_investment_pool_efficiency_mult = 0.10
		state_clergymen_investment_pool_efficiency_mult = 0.10
		state_farmers_investment_pool_efficiency_mult = 0.10
		state_capitalists_investment_pool_efficiency_mult = 0.05
		country_farmers_pol_str_mult = 0.05
	}
}

# --- law_ecclesiarchy  (zrodlo: Laws+/00_distribution_of_power.txt) ---
REPLACE:law_ecclesiarchy = { # 그리스계 국가	# Laws +
	group = lawgroup_distribution_of_power
	parent = law_oligarchy

	icon = "gfx/interface/icons/law_icons/ecclesiarchy.dds"

	progressiveness = -50

	is_visible = {
		country_has_primary_culture = cu:greek
		country_has_state_religion = rel:orthodox
		has_law = law_type:law_state_religion
	}

	requires_law_or = {
		law_state_religion
	}

	can_enact = {
		ig:ig_devout ?= {
			is_in_government = yes
		}
	}

	on_activate = {
		#set_only_legal_party_from_ig = ig:ig_devout
		custom_tooltip = {
			text = sps_government_reshuffle_tt
		}
		handle_possible_absent_caciquismo_variables = yes
		scripted_effect_parties_emergence = yes
		inherit_caciquismo_effect = yes
		if = {
			limit = {
				has_law = law_type:law_monarchy
				has_variable = king_in_parliament_possible
				NOT = {
					has_journal_entry = je_king_in_parliament
				}
			}
			custom_tooltip = {
				text = king_in_parliament_tt
				add_journal_entry = { type = je_king_in_parliament }
			}
		}
	}

	modifier = {
		country_legitimacy_govt_total_votes_add = 40
		country_legitimacy_govt_total_clout_add = 100
		country_legitimacy_govt_size_add = 1
		country_officers_pol_str_mult = 0.15
		country_aristocrats_pol_str_mult = 0.15
		country_capitalists_pol_str_mult = 0.15
		country_clergymen_pol_str_mult = 0.50
		country_clergymen_voting_power_add = 100
		country_authority_add = 100
		country_electoral_confidence_impact_mult = -0.4
		country_rigidity_baseline_add = 15	# BPM compat
	}

	can_impose = {
		OR = {
			AND = {
				scope:target_country = {
					country_has_primary_culture = cu:greek
				}
				is_in_same_power_bloc = scope:target_country
				AND = {
					modifier:country_can_impose_same_lawgroup_distribution_of_power_in_power_bloc_bool = yes
					has_law = scope:law
				}
			}
			can_impose_law_default = yes
		}
	}

	ai_enact_weight_modifier = { #청원
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}

	ai_impose_chance = {
		value = 0

		if = {
			limit = {
				has_law = law_type:law_ecclesiarchy
				OR = {
					AND = {
						power_bloc ?= { has_identity = identity:identity_ideological_union }
						has_strategy = ai_strategy_conservative_agenda
					}
					has_strategy = ai_strategy_reactionary_agenda
				}
				scope:target_country = {
					NOR = { # 종속국의 법이 종주국이 부과하려는 법과 너무 간극이 큰가?
						has_law_or_variant = law_type:law_autocracy
						has_law_or_variant = law_type:law_landed_voting
					}
				}
			}

			add = base_impose_law_weight
		}
	}
}

# --- law_garde_nationale  (zrodlo: Laws+/00_internal_security.txt) ---
REPLACE:law_garde_nationale = { # 프랑스	# Laws +
	group = lawgroup_internal_security
	parent = law_national_guard
	icon = "gfx/interface/icons/law_icons/la_garde_nationale.dds"

	progressiveness = 25

	unlocking_technologies = {
		law_enforcement
	}

	modifier = {
		country_soldiers_pol_str_mult = 0.25
		country_rigidity_baseline_add = 2	# BPM compat
	}

	is_visible = {
		country_has_primary_culture = cu:french
	}

	institution = institution_home_affairs
	institution_modifier = {
		interest_group_ig_petty_bourgeoisie_pol_str_mult = 0.05
		state_conscription_rate_add = 0.005
		political_movement_radicalism_add = -0.03
		state_radicals_from_political_movements_mult = -0.03
		state_loyalists_from_political_movements_mult = 0.03
		country_coup_resistance_add = 0.5
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}
}

# --- law_gendarmerie  (zrodlo: Laws+/00_internal_security.txt) ---
REPLACE:law_gendarmerie = { # 헌병군	# Laws +
	group = lawgroup_internal_security
	icon = "gfx/interface/icons/law_icons/gendarmerie.dds"

	progressiveness = 50

	unlocking_technologies = {
		centralization
	}

	disallowing_laws = {
		law_national_militia
		law_disarmed_nation
	}

	can_enact = {
		has_technology_researched = law_enforcement
	}

	modifier = {
		country_radicals_from_conquest_mult = -0.2
		country_officers_pol_str_mult = 0.5
		country_rigidity_baseline_add = 3	# BPM compat
	}

	institution = institution_home_affairs
	institution_modifier = {
		interest_group_ig_armed_forces_pol_str_mult = 0.05
		political_movement_radicalism_add = -0.04
		country_revolution_progress_mult = -0.05
		country_secession_progress_mult = -0.05
		country_support_separatism_resistance_mult = 0.1
		country_amenability_add = 3
		country_coup_resistance_add = 0.5
	}

	ai_enact_weight_modifier = { #청원
		value = 0
		
		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}
}

# --- law_no_election  (zrodlo: Laws+/ballot_system.txt) ---
REPLACE:law_no_election = { # 선거 없음
	group = lawgroup_ballot_system

	icon = "gfx/interface/icons/law_icons/no_voting.dds"

	progressiveness = -100

	disallowing_laws = {
		law_ecclesiarchy
		law_landed_voting
		law_wealth_voting
		law_census_voting
		law_weighted_universal_voting
		law_universal_suffrage
		law_single_party_state
		law_anarchy
	}

	can_enact = {
	}

	modifier = {
		country_opposition_ig_approval_add = -3
		country_authority_add = 100
		country_rigidity_baseline_add = 10	# BPM compat
	}

	ai_enact_weight_modifier = { #청원
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}
}

# --- law_privatized_police  (zrodlo: Laws+/01_policing_lp.txt) ---
REPLACE:law_privatized_police = { # 경찰 민영화	# Laws +
	group = lawgroup_policing

	icon = "gfx/interface/icons/law_icons/private_security_system.dds"

	progressiveness = -100

	disallowing_laws = {
		law_no_police
		law_cooperative_ownership
		law_command_economy
	}

	can_enact = {
		any_interest_group = {
			is_interest_group_type = ig_industrialists
			is_powerful = yes
		}
	}

	modifier = {
		country_institution_cost_institution_police_mult = -0.4
		country_institution_police_max_investment_add = 2
		country_rigidity_baseline_add = 2	# BPM compat
	}

	institution = institution_police
	institution_modifier = {
		state_political_strength_from_wealth_mult = 0.1
		state_pop_support_movement_slave_revolt_mult = -0.05
		state_pop_support_movement_land_reform_mult = -0.05
		state_pop_support_movement_labor_mult = -0.05
		state_pop_support_movement_communist_mult = -0.05
		state_pop_support_movement_anarchist_mult = -0.05
		state_pop_support_movement_socialist_mult = -0.05
		state_mortality_turmoil_mult = 0.002
	}

	can_impose = {
		OR = {
			AND = {
				is_in_same_power_bloc = scope:target_country
				AND = {
					modifier:country_can_impose_same_lawgroup_policing_in_power_bloc_bool = yes
					has_law = scope:law
				}
			}
			can_impose_law_default = yes
		}
	}

	ai_will_do = {
		exists = ruler
		ruler = {
			has_ideology = ideology:ideology_anarcho_liberal
		}
	}

	ai_enact_weight_modifier = { #청원
		value = 0

		if = {
			limit = {
				has_law_or_variant = law_type:law_no_police
			}
			add = 50
		}

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}

		if = {
			limit = {
				has_journal_entry = je_hawaii
			}
			add = 1000
		}
	}

	ai_impose_chance = {
		value = 0

		if = {
			limit = {
				has_law = law_type:law_privatized_police
				power_bloc ?= { has_principle_group = principle_group:principle_group_police_coordination }
			}
			add = base_impose_law_weight
		}

		if = {
			limit = {
				has_law = law_type:law_privatized_police
				scope:target_country = {
					has_law_or_variant = law_type:law_no_police
				}
				power_bloc ?= { has_principle_group = principle_group:principle_group_police_coordination }
			}
			add = 10
		}

		if = {
			limit = {
				scope:target_country = {
					c:BIC ?= this
					OR = {
						has_journal_entry = je_british_dictate_plantations
						has_journal_entry = je_british_dictate_military
						has_journal_entry = je_british_dictate_universities
					}
				}
			}
			multiply = 0
		}
	}
}

# --- law_proletariat_dictatorship  (zrodlo: Laws+/00_distribution_of_power.txt) ---
REPLACE:law_proletariat_dictatorship = { # 프롤레타리아 독재	# Laws +
	group = lawgroup_distribution_of_power

	icon = "gfx/interface/icons/law_icons/proletariat_dictatorship.dds"

	progressiveness = 0

	unlocking_technologies = {
		socialism
	}

	disallowing_laws = {
		law_monarchy
		law_chiefdom
	}

	modifier = {
		country_legitimacy_headofstate_add = 10
		country_legitimacy_govt_total_clout_add = 100
		state_political_strength_from_wealth_mult = -0.75
		country_legitimacy_govt_size_add = 1
		country_laborers_pol_str_mult = 0.5
		country_machinists_pol_str_mult = 0.75
		country_peasants_pol_str_mult = 0.25
		country_rigidity_baseline_add = 10	# BPM compat
	}

	can_enact = {
		trigger_if = {
			limit = {
				c:BIC ?= ROOT
				is_subject = yes
				NOT = { has_law = scope:law }
			}
			custom_tooltip = {
				text = BIC_not_able_to_change_distribution_of_power_tt
				has_variable = britain_granted_dop_permission
			}
		}
	}

	on_activate = {
		scripted_effect_parties_disappearence = yes
		handle_possible_absent_caciquismo_variables = yes
		cleanup_voting_strength_modifiers = yes
		if = {
			limit = {
				NOT = { has_law = law_type:law_direct_democracy }
			}
			activate_law = law_type:law_direct_democracy
		}
	}

	can_impose = {
		OR = {
			AND = {
				is_in_same_power_bloc = scope:target_country
				AND = {
					modifier:country_can_impose_same_lawgroup_distribution_of_power_in_power_bloc_bool = yes
					has_law = scope:law
				}
			}
			AND = { # 프롤레타리아 독재제 국가 또는 평의회 공화국만이 종속국에게 프롤레타리아 독재제를 강요할 수 있음
				OR = {
					has_law = scope:law
					has_law = law_type:law_council_republic
				}
				can_impose_law_default = yes
			}
		}
	}

	ai_enact_weight_modifier = { #청원
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}

	ai_impose_chance = {
		value = 0

		if = {
			limit = {
				has_law = law_type:law_proletariat_dictatorship
				has_strategy = ai_strategy_egalitarian_agenda	
				scope:target_country = { # 종속국의 법이 종주국이 부과하려는 법과 너무 간극이 큰가?
					NOT = {
						has_law_or_variant = law_type:law_single_party_state
					}
				}
			}

			add = base_impose_law_weight
		}

		if = {
			limit = {
				scope:target_country = {
					c:BIC ?= ROOT
					OR = {
						has_journal_entry = je_british_dictate_plantations
						has_journal_entry = je_british_dictate_military
						has_journal_entry = je_british_dictate_universities
					}
				}
			}
			multiply = 0
		}
	}
}

# --- law_sakoku_shugi  (zrodlo: Laws+/01_trade_policy_lp.txt) ---
REPLACE:law_sakoku_shugi = { # 일본 & 한국	# Laws +
	group = lawgroup_trade_policy
	parent = law_isolationism # 상위 법률로부터 이념적 입장을 물려 받음

	icon = "gfx/interface/icons/law_icons/sakoku_shugi.dds"

	progressiveness = -25

	is_visible = {
		OR = {
			any_scope_state = {
				state_region = s:STATE_KYUSHU
				is_largest_state_in_region = yes
			}
			owns_treaty_port_in = STATE_KYUSHU
		}
		OR = {
			AND = {
				c:JAP ?= this
				NOT = { has_law = law_type:law_sakoku }
			}
			AND = {
				c:KOR ?= this
				NOT = { has_law = law_type:law_isolationism }
			}
		}
		
	}

	modifier = {
		country_authority_mult = 0.25
		country_society_tech_spread_mult = -0.2
		state_trade_capacity_mult = -0.5
		state_tariff_import_add = 0.5
		state_tariff_export_add = 0.5
		state_subvention_export_add = 0.2
		state_subvention_import_add = 0.2
		country_leverage_resistance_mult = 0.75
		country_support_separatism_resistance_mult = 0.25

		country_disallow_trade_outside_kyushu_bool = yes
		state_trade_center_max_limit_add = 5

		country_nationalization_cost_non_members_mult = -0.75
		country_rigidity_baseline_add = 10	# BPM compat
	}

	ai_will_do = {
		ruler = {
			OR = {
				has_ideology = ideology:ideology_traditionalist
				has_ideology = ideology:ideology_vanguardist
				has_ideology = ideology:ideology_shojoi
			}
		}
	}

	on_activate = {
		if = {
			limit = {
				NOT = {
					has_law = law_type:law_closed_borders
				}
			}
			custom_tooltip = internal_controls_strengthened_tt
			activate_law = law_type:law_closed_borders
		}
		if = {
			limit = {
				c:JAP ?= this
			}
			if = {
				limit = {
					is_interest_group_type = ig_landowners
					has_ideology = ideology:ideology_japan_hierarchic_2
				}
				custom_tooltip = {
					text = lp_landowners_stop_supporting_sakoku_tt
					ig:ig_landowners ?= {
						remove_ideology = ideology_japan_hierarchic_2
						add_ideology = ideology_japan_hierarchic
					}
				}
			}
			if = {
				limit = {
					NOR = {
						owns_entire_state_region = STATE_KYUSHU
						has_claim = s:STATE_KYUSHU
					}
				}
				s:STATE_KYUSHU = {
					add_claim = ROOT
				}
			}
			if = { # (큐슈 제외)일본의 시작 영토를 모두 보유 중일 경우
				limit = {
					owns_entire_state_region = STATE_KYOTO
					owns_entire_state_region = STATE_KANTO
					owns_entire_state_region = STATE_KANSAI
					owns_entire_state_region = STATE_TOKAI
					owns_entire_state_region = STATE_CHUGOKU
					owns_entire_state_region = STATE_HOKUSHINETSU
					owns_entire_state_region = STATE_TOHOKU
					owns_entire_state_region = STATE_SHIKOKU
				}
				custom_tooltip = {
					text = lp_rising_seikanron_tt
					if = { # 대일본 명분 부여
						limit = {
							NOR = {
								owns_entire_state_region = STATE_HOKKAIDO
								has_claim = s:STATE_HOKKAIDO
							}
						}
						s:STATE_HOKKAIDO = {
							add_claim = ROOT
						}
					}
					if = {
						limit = {
							NOR = {
								owns_entire_state_region = STATE_SAKHALIN
								has_claim = s:STATE_SAKHALIN
							}
						}
						s:STATE_SAKHALIN = {
							add_claim = ROOT
						}
					}
					if = {
						limit = {
							NOR = {
								owns_entire_state_region = STATE_RYUKYU_ISLANDS
								has_claim = s:STATE_RYUKYU_ISLANDS
							}
						}
						s:STATE_RYUKYU_ISLANDS = {
							add_claim = ROOT
						}
					}
					if = { # 정한론
						limit = {
							NOR = {
								owns_entire_state_region = STATE_BUSAN
								has_claim = s:STATE_BUSAN
							}
						}
						s:STATE_BUSAN = {
							add_claim = ROOT
						}
					}
					if = {
						limit = {
							NOR = {
								owns_entire_state_region = STATE_SEOUL
								has_claim = s:STATE_SEOUL
							}
						}
						s:STATE_SEOUL = {
							add_claim = ROOT
						}
					}
					if = {
						limit = {
							NOR = {
								owns_entire_state_region = STATE_SARIWON
								has_claim = s:STATE_SARIWON
							}
						}
						s:STATE_SARIWON = {
							add_claim = ROOT
						}
					}
					if = {
						limit = {
							NOR = {
								owns_entire_state_region = STATE_PYONGYANG
								has_claim = s:STATE_PYONGYANG
							}
						}
						s:STATE_PYONGYANG = {
							add_claim = ROOT
						}
					}
					if = {
						limit = {
							NOR = {
								owns_entire_state_region = STATE_YANGHO
								has_claim = s:STATE_YANGHO
							}
						}
						s:STATE_YANGHO = {
							add_claim = ROOT
						}
					}
				}
			}
			else = { # 잃어버린 본토가 있다면 수복 명분 부여
				custom_tooltip = {
					text = lp_restore_japan_tt
					if = {
						limit = {
							NOR = {
								owns_entire_state_region = STATE_KYOTO
								has_claim = s:STATE_KYOTO
							}
						}
						s:STATE_KYOTO = {
							add_claim = ROOT
						}
					}
					if = {
						limit = {
							NOR = {
								owns_entire_state_region = STATE_KANTO
								has_claim = s:STATE_KANTO
							}
						}
						s:STATE_KANTO = {
							add_claim = ROOT
						}
					}
					if = {
						limit = {
							NOR = {
								owns_entire_state_region = STATE_KANSAI
								has_claim = s:STATE_KANSAI
							}
						}
						s:STATE_KANSAI = {
							add_claim = ROOT
						}
					}
					if = {
						limit = {
							NOR = {
								owns_entire_state_region = STATE_TOKAI
								has_claim = s:STATE_TOKAI
							}
						}
						s:STATE_TOKAI = {
							add_claim = ROOT
						}
					}
					if = {
						limit = {
							NOR = {
								owns_entire_state_region = STATE_CHUGOKU
								has_claim = s:STATE_CHUGOKU
							}
						}
						s:STATE_CHUGOKU = {
							add_claim = ROOT
						}
					}
					if = {
						limit = {
							NOR = {
								owns_entire_state_region = STATE_HOKUSHINETSU
								has_claim = s:STATE_HOKUSHINETSU
							}
						}
						s:STATE_HOKUSHINETSU = {
							add_claim = ROOT
						}
					}
					if = {
						limit = {
							NOR = {
								owns_entire_state_region = STATE_TOHOKU
								has_claim = s:STATE_TOHOKU
							}
						}
						s:STATE_TOHOKU = {
							add_claim = ROOT
						}
					}
					if = {
						limit = {
							NOR = {
								owns_entire_state_region = STATE_SHIKOKU
								has_claim = s:STATE_SHIKOKU
							}
						}
						s:STATE_SHIKOKU = {
							add_claim = ROOT
						}
					}
				}
			}
		}
	}

	on_deactivate = {
		if = {
			limit = {
				has_law = law_type:law_closed_borders
			}
			custom_tooltip = internal_controls_relaxed_tt
			activate_law = law_type:law_migration_controls
		}
		if = {
			limit = {
				c:JAP ?= this
			}
			custom_tooltip = {
				text = landowners_stop_supporting_sakoku_tt
				ig:ig_landowners ?= {
					remove_ideology = ideology_japan_hierarchic
					add_ideology = ideology_japan_hierarchic_2
				}
			}
			if = {
				limit = {
					has_claim = s:STATE_BUSAN
				}
				s:STATE_BUSAN = {
					remove_claim = ROOT
				}
			}
			if = {
				limit = {
					has_claim = s:STATE_SEOUL
				}
				s:STATE_SEOUL = {
					remove_claim = ROOT
				}
			}
			if = {
				limit = {
					has_claim = s:STATE_SARIWON
				}
				s:STATE_SARIWON = {
					remove_claim = ROOT
				}
			}
			if = {
				limit = {
					has_claim = s:STATE_PYONGYANG
				}
				s:STATE_PYONGYANG = {
					remove_claim = ROOT
				}
			}
			if = {
				limit = {
					has_claim = s:STATE_YANGHO
				}
				s:STATE_YANGHO = {
					remove_claim = ROOT
				}
			}
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}
}

# --- law_secret_ballot  (zrodlo: Laws+/ballot_system.txt) ---
REPLACE:law_secret_ballot = { # 비밀 투표
	group = lawgroup_ballot_system

	icon = "gfx/interface/icons/law_icons/secret_ballot.dds"

	progressiveness = 100

	unlocking_laws = {
		law_wealth_voting
		law_census_voting
		law_weighted_universal_voting
		law_universal_suffrage
		law_single_party_state
	}

	modifier = {
		country_legitimacy_base_add = 5
		country_legitimacy_govt_total_votes_add = 15
		country_legitimacy_ideological_incoherence_mult = -0.15
		country_rigidity_baseline_add = -5	# BPM compat
	}

	on_activate = {
	}

	ai_enact_weight_modifier = { #청원
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}
}

# --- law_semi_constitutional  (zrodlo: Laws+/ballot_system.txt) ---
REPLACE:law_semi_constitutional = { # 외형적 입헌제
	group = lawgroup_ballot_system

	icon = "gfx/interface/icons/law_icons/semi_constitutional.dds"

	progressiveness = -50

	unlocking_laws = {
		law_landed_voting
		law_wealth_voting
		law_census_voting
		law_weighted_universal_voting
		law_universal_suffrage
		law_single_party_state
	}

	disallowing_laws = {
		law_parliamentary_republic
		law_council_republic
	}

	modifier = {
		country_legitimacy_headofstate_add = 10
		country_legitimacy_govt_total_votes_add = -15
		country_legitimacy_govt_total_clout_add = 15
		country_voting_power_base_add = -20
		country_authority_add = 100
		country_electoral_confidence_impact_mult = -0.1
		interest_group_in_government_attraction_mult = 0.2

		country_opposition_ig_approval_add = -2
		country_aristocrats_pol_str_mult = 0.25
		country_capitalists_pol_str_mult = 0.25
		country_officers_pol_str_mult = 0.25
		country_clergymen_pol_str_mult = 0.25
		country_rigidity_baseline_add = 5	# BPM compat
	}

	ai_enact_weight_modifier = { #청원
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}
}

# --- law_state_capitalism  (zrodlo: Laws+/01_economic_system.txt) ---
REPLACE:law_state_capitalism = { # 국가자본주의	# Laws +
	group = lawgroup_economic_system

	icon = "gfx/interface/icons/law_icons/state_capitalism.dds"

	progressiveness = 25

	disallowing_laws = {
		law_serfdom
		law_anarchy
		law_corporatocracy
		law_factory_councils
		law_women_in_the_fields
	}

	on_activate = {
	}

	unlocking_technologies = {
		centralization
	}

	modifier = {
		state_bureaucrats_investment_pool_efficiency_mult = 0.25
		state_shopkeepers_investment_pool_efficiency_mult = -0.25
		state_capitalists_investment_pool_efficiency_mult = -0.25
		building_nationalization_cost_mult = -0.5
		building_nationalization_radicals_mult = -0.25
		building_nationalization_investment_return_add = 0.75
		country_government_dividends_efficiency_add = 0.35
		country_government_dividends_reinvestment_add = 0.1
		country_private_construction_allocation_mult = 0.25
		building_company_government_dividends_add = 0.25
		country_disable_nationalization_without_compensation_bool = yes

		building_group_bg_manufacturing_tax_mult = 0.1
		building_group_bg_extraction_tax_mult = 0.1

		country_free_charters_add = 1
	}

	ai_enact_weight_modifier = { #청원
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}

		# AI가 일정 사회 기술 수준에 도달하면 전통주의를 버리고 농본주의나 간섭주의로 전환해야 함
		if = {
			limit = {
				OR = {
					has_technology_researched = civilizing_mission
					has_technology_researched = human_rights
					has_technology_researched = pan-nationalism
				}
				has_law_or_variant = law_type:law_traditionalism
			}

			add = 1000
		}

		if = {
			limit = {
				OR = {
					has_journal_entry = je_sick_man_bureaucracy
					has_journal_entry = je_meiji_main
					has_journal_entry = je_great_reforms_serfdom
					has_journal_entry = je_hawaii
				}
				has_law_or_variant = law_type:law_traditionalism
			}
			add = 10000 # 탄지마트/메이지 유산 AI의 최우선 과제는 전통주의에서 벗어나는 것임
		}

		# AI는 적절한 환경이 조성된다면 전통주의에서 벗어나려고 노력해야 함
		if = {
			limit = {
				has_law_or_variant = law_type:law_traditionalism
				NOT = { has_strategy = ai_strategy_reactionary_agenda }
				OR = {
					NOT = { has_strategy = ai_strategy_conservative_agenda }
					any_political_movement = {
						law_enactment_stance = {
							law = law_type:law_state_capitalism
							value > neutral
						}
						political_movement_support >= 0.10
					}
				}
			}
			add = 1000
		}
	}
	institution = institution_economy	# BPM compat
	institution_modifier = {
		state_construction_mult = 0.15
		state_bureaucrats_investment_pool_efficiency_mult = 0.08
		country_bureaucrats_pol_str_mult = 0.05
		country_government_dividends_efficiency_add = 0.15
		building_company_government_dividends_add = 0.10
	}
}

# --- law_weighted_universal_voting  (zrodlo: Laws+/00_distribution_of_power.txt) ---
REPLACE:law_weighted_universal_voting = { # 차등보편선거권	# Laws +
	group = lawgroup_distribution_of_power

	icon = "gfx/interface/icons/law_icons/weighted_universal_voting.dds"

	progressiveness = 75

	unlocking_technologies = {
		egalitarianism
	}

	disallowing_laws = {
		law_chiefdom
		law_command_economy
	}

	modifier = {
		country_legitimacy_govt_total_votes_add = 95
		country_legitimacy_govt_total_clout_add = 40
		country_voting_power_base_add = 50
		country_legitimacy_govt_size_add = 1
		country_aristocrats_voting_power_add = 50
		country_capitalists_voting_power_add = 50
		country_clerks_voting_power_add = -20
		country_farmers_voting_power_add = -20
		country_laborers_voting_power_add = -20
		country_machinists_voting_power_add = -20
		country_peasants_voting_power_add = -20
		country_soldiers_voting_power_add = -20
		country_authority_add = 25
		country_rigidity_baseline_add = -18	# BPM compat
	}

	can_enact = {
		trigger_if = {
			limit = {
				c:BIC ?= ROOT
				is_subject = yes
				NOT = { has_law = scope:law }
			}
			custom_tooltip = {
				text = BIC_not_able_to_change_distribution_of_power_tt
				has_variable = britain_granted_dop_permission
			}
		}
	}

	on_activate = {
		if = {
			limit = {
				game_date > 1836.1.1
				country_has_voting_franchise = yes
			}
			initialize_electoral_confidence = yes
		}
		random_law = {
			limit = {
				type = law_type:law_census_voting
			}
			save_scope_as = relevant_voting_law
		}
		handle_possible_absent_caciquismo_variables = yes
		scripted_effect_parties_emergence = yes
		inherit_caciquismo_effect = yes
		inherit_free_elections_effect = yes
		if = {
			limit = {
				has_law = law_type:law_monarchy
				has_variable = king_in_parliament_possible
				NOT = {
					has_journal_entry = je_king_in_parliament
				}
			}
			custom_tooltip = {
				text = king_in_parliament_tt
				add_journal_entry = { type = je_king_in_parliament }
			}
		}
		if = {
			limit = { has_law = law_type:law_no_election }
			if = {
				limit = { country_has_monarchy_law = yes }
				activate_law = law_type:law_semi_constitutional
			}
			else = { activate_law = law_type:law_non_secret_ballot }
		}
	}

	can_impose = {
		country_has_voting_franchise = yes # 투표권이 있는 국가만이 다른 국가에 민주주의를 주입할 수 있음.
		OR = {
			AND = {
				is_in_same_power_bloc = scope:target_country
				AND = {
					modifier:country_can_impose_same_lawgroup_distribution_of_power_in_power_bloc_bool = yes
					has_law = scope:law
				}
			}
			can_impose_law_default = yes
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}

		if = {
			limit = {
				owner ?= {
					has_journal_entry = je_zaibatsu
					has_variable = ai_zaibatsu_curtail
				}
			}
			add = 1000
		}
	}

	ai_impose_chance = {
		value = 0

		if = {
			limit = {
				has_law = law_type:law_weighted_universal_voting
				OR = {
					has_strategy = ai_strategy_progressive_agenda
					has_strategy = ai_strategy_egalitarian_agenda
				}
				scope:target_country = { # 종속국의 법이 종주국이 부과하려는 법과 너무 간극이 큰가?
					NOR = { 
						has_law_or_variant = law_type:law_census_voting
						has_law_or_variant = law_type:law_universal_suffrage 
					}
				}
			}

			add = base_impose_law_weight
		}

		if = {
			limit = {
				scope:target_country = {
					c:BIC ?= ROOT
					OR = {
						has_journal_entry = je_british_dictate_plantations
						has_journal_entry = je_british_dictate_military
						has_journal_entry = je_british_dictate_universities
					}
				}
			}
			multiply = 0
		}
	}
}

